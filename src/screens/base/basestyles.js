import {
    StyleSheet
} from 'react-native';

const baseStyles = StyleSheet.create({
    mediButton: {
        width: 100,
        height: 45,
        backgroundColor: 'green'
    }
})

export default baseStyles